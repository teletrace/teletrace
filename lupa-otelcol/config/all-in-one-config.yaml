receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  batch:

exporters:
  sqlite:

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [sqlite]
