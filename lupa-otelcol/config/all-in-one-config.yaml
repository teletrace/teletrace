receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  batch:

exporters:
  sqlite:
    db:
      path: ""

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [sqlite]
