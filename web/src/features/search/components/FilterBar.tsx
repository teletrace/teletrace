import { Paper, Typography } from "@mui/material";

export const FilterBar = () => {
  return (
    <Paper sx={{ width: "100%" }}>
      <Typography>Filter Bar goes here</Typography>
    </Paper>
  );
};
