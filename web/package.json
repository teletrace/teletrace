{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.3"
  },
  "scripts": {
    "start": "npx lerna run start",
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "lint": "npx lerna run lint"
  }
}
