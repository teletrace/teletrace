{
  "name": "lupa",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.3"
  },
  "scripts": {
    "start": "npx lerna run start",
    "start:lupa-ui": "npx lerna run --scope lupa-ui start",
    "build": "npx lerna run build",
    "build:lupa-ui": "npx lerna run --scope lupa-ui build",
    "test": "npx lerna run test",
    "test:lupa-ui": "npx lerna run --scope lupa-ui test",
    "lint": "npx lerna run lint",
    "lint:lupa-ui": "npx lerna run --scope lupa-ui lint"
  }
}
